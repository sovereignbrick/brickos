[package]
name = "brickos-db"
version = "0.1.0"
edition = "2021"
description = "BrickOS shared database models — User, Organization, Role types"
license = "AGPL-3.0"

[dependencies]
chrono = { workspace = true }
serde = { workspace = true }
uuid = { workspace = true }
sqlx = { workspace = true }
