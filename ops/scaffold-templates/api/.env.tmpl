# ${APP_TITLE} -- Local Development
# Auto-generated by scaffold. Ports: API=${PROD_PORT}, Frontend=${FRONTEND_PORT}, DB=${DEV_DB_PORT}

${PREFIX_UPPER}_DATABASE_URL=postgres://brickos:brickos@localhost:${DEV_DB_PORT}/${PREFIX}
${PREFIX_UPPER}_PLATFORM_DATABASE_URL=postgres://brickos:brickos@localhost:${DEV_DB_PORT}/${PREFIX}
${PREFIX_UPPER}_JWT_SECRET=dev-jwt-secret-change-in-production-000000000000000
${PREFIX_UPPER}_ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
${PREFIX_UPPER}_HOST=0.0.0.0
${PREFIX_UPPER}_PORT=${PROD_PORT}
${PREFIX_UPPER}_FRONTEND_URL=http://localhost:${FRONTEND_PORT}
${PREFIX_UPPER}_API_BASE_URL=http://localhost:${PROD_PORT}
${PREFIX_UPPER}_PRODUCT_NAME=${APP_TITLE}
${PREFIX_UPPER}_SUPPORT_EMAIL=dev@localhost
DEPLOY_ENVIRONMENT=development
RUST_LOG=info,${CRATE_NAME}=debug
